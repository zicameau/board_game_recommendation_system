# TLS at origin (boardlore.com). For Cloudflare "Full (strict)", keep certificates valid here.
# Let's Encrypt registration email — must be valid. Set env CADDY_EMAIL (see .env.deploy on the VPS
# or optional GitHub secret CADDY_EMAIL + deploy merge).
{
	email {$CADDY_EMAIL}
}

boardlore.com, www.boardlore.com {
	encode gzip
	reverse_proxy app:8000
}
