# Runtime + test dependencies for the FastAPI web app.
# Canonical source of truth is pyproject.toml; this file is kept in sync for
# tooling that expects requirements.txt (graders, simple pip workflows).
#
# Install:
#     pip install -r requirements.txt
#
# For the training notebook, see notebooks/requirements.txt.

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-multipart>=0.0.17
itsdangerous>=2.2.0

# --- Database / migrations ---
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.9

# --- Config / auth ---
pydantic-settings>=2.6.0
httpx>=0.28.0
PyJWT[crypto]>=2.9.0

# --- Model serving (numpy-only inference, no Torch at request time) ---
numpy>=2.0.0
pandas>=2.2.0
pyarrow>=18.0.0

# --- Tests ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
